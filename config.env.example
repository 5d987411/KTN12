# KTN12 Configuration
# ====================
# Edit these values for your environment
# 
# IMPORTANT: Set MINING_ADDRESS before starting the miner!

# Get the directory where this script is located
KTN12_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Rusty-Kaspa directory (tn12 build) - change if your rusty-kaspa is elsewhere
RUSTY_KASPA_DIR="${RUSTY_KASPA_DIR:-$HOME/rusty-kaspa-tn12}"

# Data directory for kaspad
KASPAD_DATA_DIR="${KASPAD_DATA_DIR:-$HOME/.kaspa-testnet12}"

# Log files - store in ktn12 directory for easy access
KASPAD_LOG="${KASPAD_LOG:-$KTN12_DIR/kaspad.log}"
MINER_LOG="${MINER_LOG:-$KTN12_DIR/miner.log}"
DASHBOARD_LOG="${DASHBOARD_LOG:-$KTN12_DIR/dashboard.log}"

# Network settings
RPC_PORT="${RPC_PORT:-16210}"
P2P_PORT="${P2P_PORT:-16311}"

# ====================
# REQUIRED: Set your mining address
# Get one from: https://kaspanet.io or use dashboard to generate
# Example: MINING_ADDRESS="kaspatest:q..."
# ====================
MINING_ADDRESS="kaspatest:qp2ltc576eyy8hag0tckl9kqk62cvfz7p2egrlczyw9978zsh322jtnrx469r"

# Number of miner threads
MINER_THREADS="${MINER_THREADS:-2}"

# Dashboard port
DASHBOARD_PORT="${DASHBOARD_PORT:-3001}"
